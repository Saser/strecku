load(
    "@com_github_bazelbuild_buildtools//buildifier:def.bzl",
    "buildifier",
)

buildifier(name = "buildifier")

load(
    "@bazel_gazelle//:def.bzl",
    "gazelle",
)

# gazelle:prefix github.com/Saser/strecku
gazelle(name = "gazelle")
